class ErrorType
{
    static NOT_FOUND_TOPCOUNT = "Resource was not found(Probably code is wrong)";
    static NOT_FOUND_DATE = "Resource was not found(Probably choosen date is weekend or holiday or code is wrong)";
    static OTHER = "Other error";
}

export default ErrorType;